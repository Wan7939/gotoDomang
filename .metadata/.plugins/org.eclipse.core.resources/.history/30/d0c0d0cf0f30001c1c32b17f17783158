package com.gotoDomang.user.bo;

import org.springframework.beans.factory.annotation.Autowired;

import com.gotoDomang.user.dao.UserDAO;
import com.gotoDomang.user.model.User;

public class UserBO {
	
	@Autowired
	private UserDAO userDAO;
	
	
	public void insertUser(String loginID, String password, String name, String email,String phonenNumber) {
		userDAO.insertUser(loginID, password, name, email, phonenNumber);
	}
	public User getUserByLoginIdAndPassword(String loginId, String password) {
		return userDAO.selectUserByLoginIdAndPassword(loginId, password);
	}
}
